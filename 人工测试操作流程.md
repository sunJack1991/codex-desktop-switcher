# Codex Desktop Switcher 人工测试操作流程

版本：V1.1  
日期：2026-09-09  
适用环境：macOS + Codex Desktop

本轮结果：PASS（用户于 2026-09-09 确认全部人工测试与 Shortcut 修改完成）

---

# 1. 测试目标

确认以下真实链路可用：

1. 保存经过人工验证的 DeepSeek 与 GPT Profile。
2. 使用命令在 GPT 与 DeepSeek 之间安全切换。
3. Codex 能正常退出和重新启动。
4. 每次切换前都存在可恢复的配置备份。
5. 两个 macOS Shortcut 可以完成一键切换。
6. 连续 20 次真实切换没有配置损坏。
7. DeepSeek API Key 不进入项目目录或测试记录。

自动测试通过不能替代本流程。模型是否真实可调用，必须由用户在 Codex 中确认。

---

# 2. 安全规则

测试过程中必须遵守：

- 不打开、复制或粘贴 API Key 到测试记录。
- 不把 `~/.codex/switcher/profiles/` 复制到项目目录。
- 不手工拼接 GPT 或 DeepSeek 配置。
- 只有当前 Provider 已经完成真实调用后，才能保存对应 Profile。
- Codex 无法正常退出时立即停止，不使用 `kill -9`。
- 任一步骤结果与本文不一致时立即停止，不继续批量测试。

项目目录应为：

```text
/Users/sunshine/Downloads/Codex_Switcher
```

---

# 3. 测试前检查

打开 Terminal：

```zsh
cd "/Users/sunshine/Downloads/Codex_Switcher"
pwd
/bin/zsh -n codex-switch.sh setup.sh tests/test-switcher.zsh tests/test-setup.zsh
./tests/test-switcher.zsh
./tests/test-setup.zsh
./setup.sh check
```

预期：

- 两个测试脚本显示 `PASS`。
- `当前 config.toml` 显示 `OK`。
- 尚未初始化时，三个 Profile 项显示 `MISSING` 是正常现象。
- 命令中不得出现真实 API Key。

记录：

| 检查项 | 结果 | 备注 |
|---|---|---|
| zsh 语法检查 | PASS / FAIL | |
| Switcher 自动测试 | PASS / FAIL | |
| Setup 自动测试 | PASS / FAIL | |
| 当前 config.toml | OK / MISSING | |

若自动测试失败，停止人工测试。

---

# 4. 安装本地脚本

执行：

```zsh
cd "/Users/sunshine/Downloads/Codex_Switcher"
./setup.sh install
./setup.sh check
```

预期：

- 显示已安装到 `~/.codex/switcher/bin/codex-switch.sh`。
- `已安装的切换脚本` 显示 `OK`。
- 还没有保存的 Profile 继续显示 `MISSING`。

每次修改项目中的 `codex-switch.sh` 后，都需要重新执行 `./setup.sh install`。

---

# 5. 保存 DeepSeek Profile

## 5.1 进入 DeepSeek

使用此前已经验证过的官方 DeepSeek 配置流程，让 Codex 进入 DeepSeek 状态。

不要根据文档手写或猜测配置。

## 5.2 人工确认 DeepSeek

重新打开 Codex，至少完成一次真实任务，例如：

```text
请只回复：DEEPSEEK_TEST_OK
```

同时确认：

- Codex 没有 Provider、认证或模型目录错误。
- 当前选择的是预期的 DeepSeek 模型。
- `deepseek-v4-flash` 能实际返回结果。

不要依赖“你是什么模型”这类自我描述判断 Provider，应以当前配置、界面状态和真实调用结果为准。

## 5.3 保存快照

只有 5.2 已通过，才执行：

```zsh
cd "/Users/sunshine/Downloads/Codex_Switcher"
./setup.sh save-deepseek --confirmed-working
./setup.sh check
```

预期：

- `DeepSeek Profile` 显示 `OK`。
- `DeepSeek models 快照` 显示 `OK`。
- 不输出配置全文或 API Key。

结果：DeepSeek Profile `PASS / FAIL`

---

# 6. 恢复并保存 GPT Profile

## 6.1 恢复 GPT

使用此前验证过的官方 Restore 流程恢复 Codex GPT 配置，然后重新打开 Codex。

## 6.2 人工确认 GPT

至少完成一次真实任务，例如：

```text
请只回复：GPT_TEST_OK
```

确认：

- 没有 Provider 或认证错误。
- 当前界面显示预期的 GPT 模型。
- GPT 能实际返回结果。

## 6.3 保存快照

只有 6.2 已通过，才执行：

```zsh
cd "/Users/sunshine/Downloads/Codex_Switcher"
./setup.sh save-gpt --confirmed-working
./setup.sh check
```

最终 `setup.sh check` 的五项都应显示 `OK`。

结果：GPT Restore `PASS / FAIL`

---

# 7. 命令行双向冒烟测试

切换会正常退出当前 Codex。执行前先保存正在编辑的内容，并结束重要任务。

## 7.1 GPT → DeepSeek

执行：

```zsh
"$HOME/.codex/switcher/bin/codex-switch.sh" deepseek
```

预期：

1. Codex 正常退出。
2. Terminal 不显示成功信息。
3. Codex 自动重新打开。
4. macOS 不显示通知、弹窗或提醒。
5. 在 Codex 中执行一次轻量任务并获得正常结果。

检查状态，仅输出 Provider 名称，不读取配置内容：

```zsh
/bin/cat "$HOME/.codex/switcher/state"
```

预期输出：`deepseek`。

结果：GPT → DeepSeek `PASS / FAIL`

## 7.2 DeepSeek → GPT

执行：

```zsh
"$HOME/.codex/switcher/bin/codex-switch.sh" gpt
```

按照 7.1 的五项标准检查，并执行一次 GPT 轻量任务。

检查状态：

```zsh
/bin/cat "$HOME/.codex/switcher/state"
```

预期输出：`gpt`。

结果：DeepSeek → GPT `PASS / FAIL`

任何一次切换失败，都不要进入连续 20 次测试。

---

# 8. 创建并验证两个 Shortcut

在 macOS「快捷指令」中创建两个快捷指令，各添加一个“运行 Shell 脚本”动作。

## Shortcut 1

名称：`Codex GPT`

命令：

```zsh
"$HOME/.codex/switcher/bin/codex-switch.sh" gpt
```

## Shortcut 2

名称：`Codex DeepSeek`

命令：

```zsh
"$HOME/.codex/switcher/bin/codex-switch.sh" deepseek
```

分别点击一次，并按照第 7 节检查：

- Codex 正常退出。
- Codex 正常重启。
- 没有通知、弹窗或成功结果。
- `state` 正确。
- 目标 Provider 能完成真实调用。

结果：

| Shortcut | 结果 | 备注 |
|---|---|---|
| Codex GPT | PASS / FAIL | |
| Codex DeepSeek | PASS / FAIL | |

两个 Shortcut 都通过后，才固定到 Dock 或桌面。

---

# 9. 连续 20 次真实切换

开始前确保当前状态为 GPT：

```zsh
/bin/cat "$HOME/.codex/switcher/state"
```

预期：`gpt`。

按照下表交替点击 Shortcut。每次必须等 Codex 完全打开后再进行下一次。

| 次数 | 目标 | 正常退出 | 正常启动 | 无提示 | state | 真实调用抽检 | 结果 |
|---:|---|---|---|---|---|---|---|
| 1 | DeepSeek | | | | | 必测 | |
| 2 | GPT | | | | | 必测 | |
| 3 | DeepSeek | | | | | | |
| 4 | GPT | | | | | | |
| 5 | DeepSeek | | | | | | |
| 6 | GPT | | | | | | |
| 7 | DeepSeek | | | | | | |
| 8 | GPT | | | | | | |
| 9 | DeepSeek | | | | | | |
| 10 | GPT | | | | | 必测 | |
| 11 | DeepSeek | | | | | 必测 | |
| 12 | GPT | | | | | | |
| 13 | DeepSeek | | | | | | |
| 14 | GPT | | | | | | |
| 15 | DeepSeek | | | | | | |
| 16 | GPT | | | | | | |
| 17 | DeepSeek | | | | | | |
| 18 | GPT | | | | | | |
| 19 | DeepSeek | | | | | 必测 | |
| 20 | GPT | | | | | 必测 | |

判定规则：

- 任一次配置错误、退出超时、启动失败或 Provider 错误，整体判定为 `FAIL`。
- 不执行“真实调用抽检”的轮次，也必须确认 Codex 主窗口已经正常出现。
- 第 20 次结束后应回到 GPT。

结果：连续 20 次真实切换 `PASS / FAIL`

---

# 10. 备份、权限与 Secret 检查

## 10.1 备份数量

```zsh
backup_files=("$HOME/.codex/switcher/backups"/config_*.toml(N))
print -r -- "config backups: ${#backup_files}"
```

预期：

- 至少存在 1 份备份。
- 完成足够多次切换后最多保留 20 份。

不要把备份内容输出到 Terminal 或测试记录。

## 10.2 权限

```zsh
/usr/bin/stat -f '%Sp %N' \
  "$HOME/.codex/switcher" \
  "$HOME/.codex/switcher/profiles" \
  "$HOME/.codex/switcher/profiles/gpt.toml" \
  "$HOME/.codex/switcher/profiles/deepseek.toml" \
  "$HOME/.codex/switcher/profiles/models.deepseek.json"
```

预期：

- `switcher` 与 `profiles`：`drwx------`。
- 三个 Profile 文件：`-rw-------`。

## 10.3 项目目录 Secret 检查

在项目目录执行：

```zsh
cd "/Users/sunshine/Downloads/Codex_Switcher"
rg -n -S 'sk-' .
find . -type f \( -name 'gpt.toml' -o -name 'deepseek.toml' -o -name 'models.deepseek.json' \) -print
```

预期：两个命令都没有输出。

不要对 `~/.codex` 执行 Secret 内容搜索，避免把真实 Key 打印到 Terminal。

结果：备份、权限与 Secret 检查 `PASS / FAIL`

---

# 11. 失败处理与恢复

如果切换失败：

1. 立即停止连续测试。
2. 不重复点击 Shortcut。
3. 不手工编辑 `config.toml`。
4. 记录失败时间、目标 Provider、错误提示和 Codex 是否仍在运行。
5. 不记录配置全文、Profile 内容或 API Key。

查看可用备份，只列文件名：

```zsh
backup_files=("$HOME/.codex/switcher/backups"/config_*.toml(N.om))
print -rl -- "${backup_files[@]:t}"
```

如果需要恢复，请先停止操作并确认要使用哪一份备份。V1 尚未提供自动 `restore` 命令，不要未经确认覆盖当前配置。

故障记录模板：

```text
时间：
操作入口：Terminal / Codex GPT Shortcut / Codex DeepSeek Shortcut
切换方向：GPT → DeepSeek / DeepSeek → GPT
错误提示：
Codex 是否正常退出：是 / 否 / 未知
Codex 是否正常启动：是 / 否 / 未知
state：gpt / deepseek / 不存在
最近备份文件名：
是否涉及 Secret 泄漏：否 / 是（不要粘贴 Secret）
```

---

# 12. 最终验收

全部满足才判定 V1 人工测试通过：

- [x] DeepSeek Profile 来自真实可用状态。
- [x] GPT Profile 来自 Restore 后真实可用状态。
- [x] GPT → DeepSeek 命令行冒烟通过。
- [x] DeepSeek → GPT 命令行冒烟通过。
- [x] `Codex GPT` Shortcut 通过。
- [x] `Codex DeepSeek` Shortcut 通过。
- [x] 连续 20 次真实切换通过。
- [x] 每次切换均有配置备份。
- [x] 备份最多保留 20 份。
- [x] 私有目录和 Profile 权限正确。
- [x] 项目目录不存在真实 Profile 或 API Key。
- [x] 测试结束后 Codex 处于 GPT 状态。

最终结论：`PASS`

测试人：  
完成时间：  
备注：
